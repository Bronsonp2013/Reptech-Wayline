# Normalize line endings to LF in the repo (deploy target is Linux Docker).
# Prevents CRLF/LF churn between Windows dev and the container.
* text=auto eol=lf

# Binary assets — never touch.
*.ico binary
*.png binary
*.jpg binary
*.webp binary
*.zip binary
